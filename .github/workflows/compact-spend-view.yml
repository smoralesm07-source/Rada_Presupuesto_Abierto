name: Legacy Compact Spend View diagnostic
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "El compactador independiente quedó retirado como publicador."
          echo "Corrected Spend Data v3 realiza la compactación después de construir y validar la semántica corregida."
