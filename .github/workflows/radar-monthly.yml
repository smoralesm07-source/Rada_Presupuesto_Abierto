name: Radar monthly analysis
on:
  workflow_dispatch:
    inputs:
      years:
        description: 'Años separados por espacio; vacío = últimos 3 bulk disponibles'
        required: false
        default: ''
  schedule:
    - cron: '31 9 10 * *'
permissions:
  contents: write
jobs:
  radar:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - uses: actions/checkout@v6
      - name: Checkout Radar CGR evidence
        uses: actions/checkout@v6
        with:
          repository: smoralesm07-source/Radar-CGR
          path: external/radar-cgr
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - name: Probe sources
        run: PYTHONPATH=src python -m radar_presupuesto.cli probe
      - name: Resolve years
        id: years
        env:
          REQUESTED: ${{ github.event.inputs.years }}
        run: |
          if [ -n "$REQUESTED" ]; then echo "years=$REQUESTED" >> "$GITHUB_OUTPUT"; exit 0; fi
          YEARS=$(python - <<'PY'
          import json
          d=json.load(open('docs/data/source_catalog.json'))
          ok=('linked','linked_available','probed_available')
          ys=[str(x['year']) for x in d.get('downloads',[]) if x.get('status') in ok]
          print(' '.join(ys[-3:]))
          PY
          )
          echo "years=$YEARS" >> "$GITHUB_OUTPUT"
      - name: Run operational radar
        run: |
          test -n "${{ steps.years.outputs.years }}"
          PYTHONPATH=src python -m radar_presupuesto.pipeline --years ${{ steps.years.outputs.years }}
      - name: Build territorial context export
        run: PYTHONPATH=src python -m radar_presupuesto.territorial_export
      - name: Rebuild spend view module artifact
        run: PYTHONPATH=src python -m radar_presupuesto.spend_view
      - name: Persist publishable summaries only
        run: |
          git config user.name "radar-presupuesto-bot"
          git config user.email "radar-presupuesto-bot@users.noreply.github.com"
          git add docs/data/dashboard.json docs/data/investigation_queue.json docs/data/cgr_correlation.json docs/data/source_catalog.json docs/data/coverage.json docs/data/quality.json docs/data/snapshot_manifest.json docs/data/territorial_context_v1.json docs/data/spend_view_v1.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "radar: corrida analítica $(date -u +%F)"
          git pull --rebase origin main
          git push origin main
