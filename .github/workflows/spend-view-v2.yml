name: Legacy Spend View v2 diagnostic
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Build Spend View v2 quedó retirado como publicador."
          echo "La vista productiva se genera exclusivamente en Corrected Spend Data v3."
          echo "Esto evita reintroducir la granularidad histórica de Área como Servicio."
