name: Search Presupuesto Abierto
on:
  workflow_dispatch:
    inputs:
      year:
        description: 'Año a consultar'
        required: true
        default: '2026'
      text:
        description: 'Texto libre (beneficiario, organismo, gasto, documento, OC, BIP)'
        required: false
        default: ''
      filters_json:
        description: 'Filtros JSON opcionales, ej. {"rut":"96875230-8","provider_only":true,"min_amount":1000000}'
        required: false
        default: '{}'
      limit:
        description: 'Máximo resultados (1-10000)'
        required: true
        default: '1000'
permissions:
  contents: read
jobs:
  search:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - name: Execute auditable search
        env:
          PYTHONPATH: src
          YEAR: ${{ inputs.year }}
          TEXT: ${{ inputs.text }}
          FILTERS: ${{ inputs.filters_json }}
          LIMIT: ${{ inputs.limit }}
        run: |
          python -m radar_presupuesto.query_job --year "$YEAR" --text "$TEXT" --filters-json "$FILTERS" --limit "$LIMIT"
      - name: Upload query result
        uses: actions/upload-artifact@v4
        with:
          name: presupuesto-abierto-search-${{ inputs.year }}
          retention-days: 30
          if-no-files-found: error
          path: |
            data/query/result.csv
            data/query/result.json
            data/query/query_metadata.json
