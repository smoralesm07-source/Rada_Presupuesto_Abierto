(()=>{'use strict';
const DATA_URL='data/investigative_findings.json';
const CASE_KEY='rigp_cases_v1';
const PAGE_SIZE=18;
const LEVEL_LABEL={ATENCION_INMEDIATA:'Atención inmediata',REVISION_PRIORITARIA:'Revisión prioritaria',SEGUIMIENTO:'Seguimiento'};
const SIGNAL_LABEL={PROVIDER_CONCENTATION:'Concentración en proveedor',PROVIDER_CONCENTRATION:'Concentración en proveedor',NEW_TO_SERIES_HIGH_SPEND:'Proveedor nuevo con gasto alto',AMOUNT_OUTLIER:'Monto fuera de patrón',POTENTIAL_FRAGMENTATION:'Operaciones similares en secuencia',EXACT_DUPLICATE_CANDIDATE:'Documentos potencialmente repetidos',PAYMENT_DELAY_OUTLIER:'Plazo de pago atípico',YEAR_END_SPIKE:'Concentración al cierre de año'};
const FAMILY_LABEL={CONVERGENCIA_MULTIFACTOR:'Convergencia de patrones',INTEGRIDAD_DOCUMENTAL_PAGOS:'Integridad documental y pagos',COMPETENCIA_ADJUDICACION:'Competencia y adjudicación',CONCENTRACION_DEPENDENCIA:'Concentración y dependencia',IRRUPCION_CAMBIO_ESCALA:'Irrupción o cambio de escala',EJECUCION_CONTRACTUAL:'Ejecución contractual',EJECUCION_PRESUPUESTARIA:'Ejecución presupuestaria',PATRON_ATIPICO:'Patrón atípico'};
const STATUS_LABEL={TRIAGE:'Triage',EN_REVISION:'En revisión',PROFUNDIZAR:'Profundizar',EXPLICADO:'Explicado',ESCALADO:'Escalado',CERRADO:'Cerrado'};
const STATUS_ORDER=['TRIAGE','EN_REVISION','PROFUNDIZAR','ESCALADO','EXPLICADO','CERRADO'];
const EVIDENCE_TEMPLATE=[['PRESUPUESTO','Hecho presupuestario','Confirmar monto, período, programa y trazabilidad del registro fuente.'],['CONTRATACION','Contratación / orden de compra','Identificar proceso, orden de compra, modalidad, competencia y adjudicación.'],['PAGO','Ejecución y pagos','Revisar recepción conforme, facturas, fechas, modificaciones y secuencia de pagos.'],['ENTIDAD','Identidad y capacidad del proveedor','Confirmar RUT/identidad, giro, antigüedad, tamaño y capacidad observable.'],['PROPIEDAD','Propiedad y actores','Resolver socios, representantes y vínculos relevantes sólo con fuentes trazables.'],['CONTEXTO','Contexto externo','Contrastar CGR, SII, sanciones u OSINT sin tratar coincidencias candidatas como hechos.']];
const S={rows:[],payload:null,view:'bandeja',query:'',level:'ACTIVE',page:0,caseId:null,entityMode:'provider'};
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const fmt=new Intl.NumberFormat('es-CL');
const money=v=>{v=Number(v||0);if(!v)return '—';const a=Math.abs(v);if(a>=1e12)return '$'+new Intl.NumberFormat('es-CL',{maximumFractionDigits:2}).format(v/1e12)+' bill.';if(a>=1e9)return '$'+new Intl.NumberFormat('es-CL',{maximumFractionDigits:1}).format(v/1e9)+' mil M';if(a>=1e6)return '$'+new Intl.NumberFormat('es-CL',{maximumFractionDigits:1}).format(v/1e6)+' M';return '$'+fmt.format(Math.round(v))};
const idOf=r=>String(r.finding_id||`${r.organization_id}|${r.provider_id}|${r.periodo}`);
const rank=r=>r.attention_level==='ATENCION_INMEDIATA'?0:r.attention_level==='REVISION_PRIORITARIA'?1:2;
const pair=r=>`${r.organization_name||r.organization_id||'Servicio'} → ${r.provider_name||r.provider_id||'Proveedor'}`;
function now(){return new Date().toISOString()}
function uuid(){if(globalThis.crypto?.randomUUID)return crypto.randomUUID();return 'case-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,10)}
function readCases(){try{const x=JSON.parse(localStorage.getItem(CASE_KEY)||'[]');return Array.isArray(x)?x:[]}catch{return []}}
function writeCases(rows){try{localStorage.setItem(CASE_KEY,JSON.stringify(rows));return true}catch{return false}}
function getCase(id){return readCases().find(c=>c.case_id===id)||null}
function patchCase(id,patch,eventType='ACTUALIZACION'){const rows=readCases();const i=rows.findIndex(c=>c.case_id===id);if(i<0)return null;const c=rows[i];Object.assign(c,patch,{updated_at:now()});c.events=Array.isArray(c.events)?c.events:[];c.events.unshift({event_type:eventType,created_at:c.updated_at});c.events=c.events.slice(0,50);rows[i]=c;writeCases(rows);return c}
function findingById(id){return S.rows.find(r=>idOf(r)===String(id))||null}
function caseForFinding(fid){return readCases().find(c=>(c.finding_ids||[]).includes(fid))||null}
function defaultHypothesis(r){switch(r.finding_family){case'COMPETENCIA_ADJUDICACION':return 'Determinar si la concentración o irrupción observada se explica por condiciones normales de contratación o si existen antecedentes objetivos que justifiquen profundizar la revisión.';case'INTEGRIDAD_DOCUMENTAL_PAGOS':return 'Determinar si la reiteración documental corresponde a hitos válidos o si existe duplicidad material que requiera revisión adicional.';case'CONVERGENCIA_MULTIFACTOR':return 'Reconstruir la relación servicio–proveedor y establecer si la convergencia de patrones tiene una explicación operativa suficiente o requiere profundización.';case'CONCENTRACION_DEPENDENCIA':return 'Evaluar si la concentración responde a estructura de mercado, contrato o convenios legítimos, y si existen factores adicionales que justifiquen atención reforzada.';case'IRRUPCION_CAMBIO_ESCALA':return 'Verificar si el cambio de escala del proveedor tiene sustento contractual y capacidad observable compatibles con el gasto registrado.';default:return 'Revisar el hallazgo en contexto, contrastando documentos y fuentes antes de formular cualquier conclusión.'}}
function ensureCase(r){const fid=idOf(r),existing=caseForFinding(fid);if(existing){S.caseId=existing.case_id;return existing}const date=new Date();const code=`RIGP-${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}${String(date.getDate()).padStart(2,'0')}-${fid.replace(/[^A-Z0-9]/gi,'').slice(-6).toUpperCase()}`;const c={case_id:uuid(),case_ref:code,title:r.finding_title||FAMILY_LABEL[r.finding_family]||'Expediente RIGP',status:'TRIAGE',priority_score:Number(r.max_priority_score||0),attention_level:r.attention_level||'SEGUIMIENTO',hypothesis:defaultHypothesis(r),scope:'',conclusion:'',organization_id:r.organization_id||'',organization_name:r.organization_name||'',provider_id:r.provider_id||'',provider_name:r.provider_name||'',period_year:r.periodo||null,finding_ids:[fid],evidence:EVIDENCE_TEMPLATE.map(([kind,label,guide])=>({kind,label,guide,status:'PENDIENTE',note:''})),notes:'',created_at:now(),updated_at:now(),events:[{event_type:'EXPEDIENTE_CREADO',created_at:now()}],source_context:{finding_family:r.finding_family,signal_types:r.signal_types||[],why_review:r.why_review||'',max_transaction_amount:r.max_transaction_amount||0}};const rows=readCases();rows.unshift(c);writeCases(rows);S.caseId=c.case_id;return c}
function filteredRows(){const q=S.query.trim().toLocaleLowerCase('es');return S.rows.filter(r=>{if(S.level==='ACTIVE'&&r.attention_level==='SEGUIMIENTO')return false;if(S.level!=='ACTIVE'&&S.level!=='ALL'&&r.attention_level!==S.level)return false;if(!q)return true;return [r.organization_name,r.organization_id,r.provider_name,r.provider_id,r.finding_title,r.finding_family,r.periodo,...(r.signal_types||[])].join(' ').toLocaleLowerCase('es').includes(q)}).sort((a,b)=>rank(a)-rank(b)||Number(b.max_priority_score||0)-Number(a.max_priority_score||0)||Number(b.signal_family_count||0)-Number(a.signal_family_count||0))}
function setStatus(text){$('status').textContent=text}
function renderKpis(){const cases=readCases(),active=cases.filter(c=>!['EXPLICADO','CERRADO'].includes(c.status)),rows=filteredRows(),immediate=rows.filter(r=>r.attention_level==='ATENCION_INMEDIATA').length,services=new Set(rows.map(r=>r.organization_id).filter(Boolean)).size,providers=new Set(rows.map(r=>r.provider_id).filter(Boolean)).size;$('kpis').innerHTML=[[active.length,'expedientes activos'],[immediate,'hallazgos inmediatos'],[services,'servicios en foco'],[providers,'proveedores en foco']].map(([v,l])=>`<div><b>${fmt.format(v)}</b><span>${l}</span></div>`).join('')}
function renderNav(){document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('on',b.dataset.view===S.view));$('filters').hidden=!['triage'].includes(S.view);$('caseBanner').hidden=!S.caseId;const c=S.caseId?getCase(S.caseId):null;$('caseBanner').innerHTML=c?`<span>Expediente activo</span><b>${esc(c.case_ref)}</b><em>${esc(STATUS_LABEL[c.status]||c.status)}</em>`:''}
function renderBandeja(){const cases=readCases().sort((a,b)=>(STATUS_ORDER.indexOf(a.status)-STATUS_ORDER.indexOf(b.status))||Number(b.priority_score||0)-Number(a.priority_score||0));const open=cases.filter(c=>!['EXPLICADO','CERRADO'].includes(c.status)),closed=cases.filter(c=>['EXPLICADO','CERRADO'].includes(c.status)),candidate=filteredRows().find(r=>!caseForFinding(idOf(r)));$('workspace').innerHTML=`<div class="section-head"><div><div class="eyebrow">Mi bandeja</div><h2>Trabajo analítico organizado por expediente</h2><p>El hallazgo sirve para descubrir. El expediente concentra hipótesis, evidencia, actores, revisión y conclusión.</p></div><span class="pilot-pill">Piloto local · persistencia del navegador</span></div>${candidate?`<section class="next-case"><div><span class="eyebrow">Siguiente sugerido para triage</span><h3>${esc(candidate.finding_title||FAMILY_LABEL[candidate.finding_family]||'Hallazgo')}</h3><p>${esc(pair(candidate))}</p><small>${esc(candidate.why_review||'Revisión recomendada.')}</small></div><div><b>${Math.round(Number(candidate.max_priority_score||0))}/100</b><button data-create-case="${esc(idOf(candidate))}">Crear expediente</button></div></section>`:''}<div class="board"><section class="panel"><div class="panel-head"><h3>Activos</h3><span>${open.length}</span></div><div class="case-list">${open.map(caseRow).join('')||'<div class="empty">Aún no hay expedientes activos.</div>'}</div></section><section class="panel"><div class="panel-head"><h3>Resueltos / cerrados</h3><span>${closed.length}</span></div><div class="case-list">${closed.slice(0,12).map(caseRow).join('')||'<div class="empty">Sin expedientes cerrados.</div>'}</div></section></div>`}
function caseRow(c){return `<button class="case-row" data-open-case="${esc(c.case_id)}"><span class="case-dot ${esc(c.status)}"></span><span class="case-row-main"><b>${esc(c.title)}</b><small>${esc(c.organization_name||c.organization_id||'Servicio')} → ${esc(c.provider_name||c.provider_id||'Proveedor')} · ${esc(c.period_year||'')}</small></span><span class="case-row-status">${esc(STATUS_LABEL[c.status]||c.status)}</span><span class="case-row-score">${Math.round(Number(c.priority_score||0))}</span></button>`}
function renderTriage(){const rows=filteredRows(),pages=Math.max(1,Math.ceil(rows.length/PAGE_SIZE));S.page=Math.max(0,Math.min(S.page,pages-1));const slice=rows.slice(S.page*PAGE_SIZE,S.page*PAGE_SIZE+PAGE_SIZE);const counts={};for(const r of rows)for(const s of r.signal_types||[])counts[s]=(counts[s]||0)+1;const diversity=Object.entries(counts).sort((a,b)=>b[1]-a[1]).map(([s,n])=>`<span>${esc(SIGNAL_LABEL[s]||s)} <b>${n}</b></span>`).join('');$('workspace').innerHTML=`<div class="section-head"><div><div class="eyebrow">Triage</div><h2>Decidir qué merece un expediente</h2><p>Prioriza por convergencia y contexto. La puntuación ordena revisión; no mide culpabilidad ni probabilidad de delito.</p></div><span>${rows.length} relaciones · página ${S.page+1}/${pages}</span></div><div class="diversity"><strong>Diversidad del lote publicado</strong>${diversity||'<span>Sin señales</span>'}</div><section class="triage-grid">${slice.map(triageCard).join('')||'<div class="empty">Sin resultados.</div>'}</section><div class="pager"><button data-page="-1" ${S.page===0?'disabled':''}>Anterior</button><button data-page="1" ${S.page===pages-1?'disabled':''}>Siguiente</button></div>`}
function triageCard(r){const c=caseForFinding(idOf(r)),signals=(r.signal_types||[]).slice(0,4).map(s=>`<span>${esc(SIGNAL_LABEL[s]||s)}</span>`).join('');return `<article class="triage-card level-${esc(r.attention_level)}"><div class="triage-top"><span>${esc(LEVEL_LABEL[r.attention_level]||r.attention_level)}</span><b>${Math.round(Number(r.max_priority_score||0))}/100</b></div><h3>${esc(r.finding_title||FAMILY_LABEL[r.finding_family]||'Hallazgo')}</h3><p class="pair">${esc(pair(r))}</p><p>${esc(r.why_review||'Relación priorizada para revisión contextual.')}</p><div class="signal-list">${signals}</div><div class="triage-meta"><span>${esc(r.periodo||'')}</span><span>${money(r.max_transaction_amount)}</span></div>${c?`<button data-open-case="${esc(c.case_id)}">Abrir expediente</button>`:`<button data-create-case="${esc(idOf(r))}">Crear expediente</button>`}</article>`}
function renderCaso(){const c=S.caseId?getCase(S.caseId):null;if(!c){$('workspace').innerHTML=`<div class="empty large"><h2>No hay expediente seleccionado</h2><p>Abre uno desde Mi bandeja o crea uno desde Triage.</p><button data-view="triage">Ir a Triage</button></div>`;return}const r=findingById((c.finding_ids||[])[0]);const evidence=(c.evidence||[]).map((e,i)=>`<div class="evidence-row"><div><b>${esc(e.label)}</b><small>${esc(e.guide)}</small></div><select data-evidence-status="${i}"><option value="PENDIENTE" ${e.status==='PENDIENTE'?'selected':''}>Pendiente</option><option value="EN_REVISION" ${e.status==='EN_REVISION'?'selected':''}>En revisión</option><option value="VERIFICADA" ${e.status==='VERIFICADA'?'selected':''}>Verificada</option><option value="DESCARTADA" ${e.status==='DESCARTADA'?'selected':''}>Descartada</option><option value="NO_DISPONIBLE" ${e.status==='NO_DISPONIBLE'?'selected':''}>No disponible</option></select></div>`).join('');$('workspace').innerHTML=`<div class="case-head"><div><div class="eyebrow">Caso</div><h2>${esc(c.title)}</h2><p>${esc(c.organization_name||c.organization_id)} → ${esc(c.provider_name||c.provider_id)} · ${esc(c.period_year||'')}</p></div><div class="case-priority"><b>${Math.round(Number(c.priority_score||0))}</b><span>prioridad de revisión</span></div></div><section class="case-status-bar">${STATUS_ORDER.filter(x=>x!=='CERRADO').map(st=>`<button data-case-status="${st}" class="${c.status===st?'on':''}">${esc(STATUS_LABEL[st])}</button>`).join('')}</section><div class="case-layout"><section class="panel span2"><div class="panel-head"><h3>Hipótesis de trabajo</h3><span>Debe poder confirmarse o descartarse</span></div><textarea id="hypothesis" rows="4">${esc(c.hypothesis||'')}</textarea><div class="save-hint">Se guarda al salir del campo.</div></section><section class="panel"><div class="panel-head"><h3>Qué sabemos</h3><span>Hechos del producto analítico</span></div><dl class="facts"><div><dt>Servicio</dt><dd>${esc(c.organization_name||c.organization_id)}</dd></div><div><dt>Proveedor</dt><dd>${esc(c.provider_name||c.provider_id)}</dd></div><div><dt>Período</dt><dd>${esc(c.period_year||'—')}</dd></div><div><dt>Monto máximo asociado</dt><dd>${money(r?.max_transaction_amount||c.source_context?.max_transaction_amount)}</dd></div><div><dt>Familia</dt><dd>${esc(FAMILY_LABEL[r?.finding_family||c.source_context?.finding_family]||r?.finding_family||c.source_context?.finding_family||'—')}</dd></div></dl></section><section class="panel"><div class="panel-head"><h3>Señales que originan la revisión</h3><span>No son conclusiones</span></div><div class="signal-list large">${(r?.signal_types||c.source_context?.signal_types||[]).map(s=>`<span>${esc(SIGNAL_LABEL[s]||s)}</span>`).join('')}</div><p class="guard-card">${esc(r?.guardrail||S.payload?.guardrail||'Un hallazgo prioriza revisión y no acredita irregularidad ni responsabilidad.')}</p></section><section class="panel span2"><div class="panel-head"><h3>Plan de evidencia</h3><span>Separar candidato, verificado y descartado</span></div><div class="evidence-list">${evidence}</div></section><section class="panel span2"><div class="panel-head"><h3>Notas del analista</h3><span>Argumentos, pendientes y decisiones</span></div><textarea id="caseNotes" rows="5" placeholder="Registra por qué se profundiza, qué se descartó y qué falta verificar…">${esc(c.notes||'')}</textarea><div class="save-hint">Se guarda al salir del campo.</div></section><section class="route-card"><div><span class="eyebrow">Siguiente paso</span><h3>Profundizar sólo donde cambie la decisión</h3><p>Usa Entidad 360° para contextualizar servicio y proveedor. Cuando la evidencia sea suficiente, genera el informe del expediente.</p></div><div><button data-view="entidad">Abrir Entidad 360°</button><button data-view="informe" class="secondary">Preparar informe</button></div></section></div>`}
function relatedProvider(c){return S.rows.filter(r=>String(r.provider_id||'')===String(c.provider_id||''))}
function relatedService(c){return S.rows.filter(r=>String(r.organization_id||'')===String(c.organization_id||''))}
function entitySummary(rows,mode,c){const names=mode==='provider'?new Map(rows.map(r=>[r.organization_id,r.organization_name||r.organization_id])):new Map(rows.map(r=>[r.provider_id,r.provider_name||r.provider_id]));const years=[...new Set(rows.map(r=>r.periodo).filter(Boolean))].sort();const families=[...new Set(rows.map(r=>FAMILY_LABEL[r.finding_family]||r.finding_family).filter(Boolean))];const signals=[...new Set(rows.flatMap(r=>r.signal_types||[]))];const immediate=rows.filter(r=>r.attention_level==='ATENCION_INMEDIATA').length;const maxScore=Math.max(0,...rows.map(r=>Number(r.max_priority_score||0)));return `<div class="entity-hero"><div><span class="eyebrow">${mode==='provider'?'Proveedor':'Servicio público'}</span><h2>${esc(mode==='provider'?(c.provider_name||c.provider_id):(c.organization_name||c.organization_id))}</h2><p>${rows.length} relaciones publicadas · ${names.size} ${mode==='provider'?'servicios':'proveedores'} · ${immediate} de atención inmediata</p></div><b>${Math.round(maxScore)}/100</b></div><div class="entity-grid-summary"><div><span>Períodos con hallazgos</span><b>${esc(years.join(', ')||'—')}</b></div><div><span>Familias observadas</span><b>${families.length}</b></div><div><span>Contrapartes</span><b>${names.size}</b></div><div><span>Señales distintas</span><b>${signals.length}</b></div></div><section class="panel"><div class="panel-head"><h3>Patrones publicados</h3><span>Contexto, no imputación</span></div><div class="signal-list large">${signals.map(s=>`<span>${esc(SIGNAL_LABEL[s]||s)}</span>`).join('')}</div><p>${families.map(esc).join(' · ')}</p></section><section class="panel"><div class="panel-head"><h3>${mode==='provider'?'Servicios relacionados':'Proveedores relacionados'}</h3><span>Hasta 20 contrapartes del producto publicado</span></div><div class="counterparty-list">${[...names.entries()].slice(0,20).map(([id,name])=>`<div><b>${esc(name)}</b><small>${esc(id)}</small></div>`).join('')||'<div class="empty">Sin contrapartes.</div>'}</div></section>`}
function renderEntidad(){const c=S.caseId?getCase(S.caseId):null;if(!c){$('workspace').innerHTML=`<div class="empty large"><h2>Entidad 360° necesita un expediente activo</h2><button data-view="bandeja">Ir a Mi bandeja</button></div>`;return}const rows=S.entityMode==='provider'?relatedProvider(c):relatedService(c);$('workspace').innerHTML=`<div class="section-head"><div><div class="eyebrow">Entidad 360°</div><h2>Contexto concentrado del expediente</h2><p>No es otra aplicación: es una vista de apoyo del caso activo.</p></div><div class="segmented"><button data-entity-mode="provider" class="${S.entityMode==='provider'?'on':''}">Proveedor</button><button data-entity-mode="service" class="${S.entityMode==='service'?'on':''}">Servicio</button></div></div>${entitySummary(rows,S.entityMode,c)}<section class="source-gap"><b>Próxima ampliación estructural</b><p>Esta vista incorporará proceso de compra/orden de compra, capacidad SII y propiedad resuelta cuando esas fuentes estén vinculadas con identificadores robustos. Las coincidencias por nombre seguirán tratándose como contexto candidato.</p></section>`}
function evidenceStats(c){const e=c.evidence||[];return {verified:e.filter(x=>x.status==='VERIFICADA').length,pending:e.filter(x=>['PENDIENTE','EN_REVISION'].includes(x.status)).length,discarded:e.filter(x=>x.status==='DESCARTADA').length,total:e.length}}
function renderInforme(){const c=S.caseId?getCase(S.caseId):null;if(!c){$('workspace').innerHTML=`<div class="empty large"><h2>No hay expediente para informar</h2><button data-view="bandeja">Ir a Mi bandeja</button></div>`;return}const st=evidenceStats(c),r=findingById((c.finding_ids||[])[0]);const conclusion=c.conclusion||'';$('workspace').innerHTML=`<div class="section-head"><div><div class="eyebrow">Informe</div><h2>Síntesis trazable del expediente</h2><p>Separa señal, evidencia y conclusión humana.</p></div><button data-copy-report>Copiar síntesis</button></div><article class="report" id="reportText"><header><b>${esc(c.case_ref)}</b><span>${esc(STATUS_LABEL[c.status]||c.status)}</span></header><h3>${esc(c.title)}</h3><dl class="facts"><div><dt>Servicio</dt><dd>${esc(c.organization_name||c.organization_id)}</dd></div><div><dt>Proveedor</dt><dd>${esc(c.provider_name||c.provider_id)}</dd></div><div><dt>Período</dt><dd>${esc(c.period_year||'—')}</dd></div><div><dt>Prioridad de revisión</dt><dd>${Math.round(Number(c.priority_score||0))}/100</dd></div></dl><section><h4>Hipótesis de trabajo</h4><p>${esc(c.hypothesis||'Sin hipótesis registrada.')}</p></section><section><h4>Origen del expediente</h4><p>${esc(r?.why_review||c.source_context?.why_review||'Hallazgo analítico priorizado.')}</p><div class="signal-list">${(r?.signal_types||c.source_context?.signal_types||[]).map(s=>`<span>${esc(SIGNAL_LABEL[s]||s)}</span>`).join('')}</div></section><section><h4>Estado de evidencia</h4><p>${st.verified} verificadas · ${st.pending} pendientes/en revisión · ${st.discarded} descartadas · ${st.total} líneas de comprobación.</p></section><section><h4>Notas del analista</h4><p>${esc(c.notes||'Sin notas registradas.')}</p></section><section><h4>Conclusión</h4><textarea id="caseConclusion" rows="5" placeholder="Registra una conclusión descriptiva, incluyendo explicaciones normales o razones para profundizar.">${esc(conclusion)}</textarea><div class="save-hint">Se guarda al salir del campo.</div></section><footer>${esc(S.payload?.guardrail||'RIGP prioriza revisión. No acredita irregularidad, delito ni responsabilidad.')}</footer></article>`}
function render(){renderNav();renderKpis();if(S.view==='bandeja')renderBandeja();else if(S.view==='triage')renderTriage();else if(S.view==='caso')renderCaso();else if(S.view==='entidad')renderEntidad();else if(S.view==='informe')renderInforme();}
function textReport(c){const r=findingById((c.finding_ids||[])[0]),st=evidenceStats(c),signals=(r?.signal_types||c.source_context?.signal_types||[]).map(s=>SIGNAL_LABEL[s]||s).join(', ');return `${c.case_ref}\n${c.title}\nEstado: ${STATUS_LABEL[c.status]||c.status}\nServicio: ${c.organization_name||c.organization_id}\nProveedor: ${c.provider_name||c.provider_id}\nPeríodo: ${c.period_year||'—'}\nPrioridad de revisión: ${Math.round(Number(c.priority_score||0))}/100\n\nHipótesis de trabajo\n${c.hypothesis||'Sin hipótesis registrada.'}\n\nOrigen del expediente\n${r?.why_review||c.source_context?.why_review||'Hallazgo analítico priorizado.'}\nSeñales: ${signals||'—'}\n\nEstado de evidencia\n${st.verified} verificadas; ${st.pending} pendientes/en revisión; ${st.discarded} descartadas.\n\nNotas\n${c.notes||'Sin notas.'}\n\nConclusión\n${c.conclusion||'Sin conclusión.'}\n\n${S.payload?.guardrail||'RIGP prioriza revisión. No acredita irregularidad, delito ni responsabilidad.'}`}
document.addEventListener('click',e=>{const v=e.target.closest('[data-view]');if(v){S.view=v.dataset.view;S.page=0;render();return}const create=e.target.closest('[data-create-case]');if(create){const r=findingById(create.dataset.createCase);if(r){ensureCase(r);S.view='caso';render()}return}const open=e.target.closest('[data-open-case]');if(open){S.caseId=open.dataset.openCase;S.view='caso';render();return}const st=e.target.closest('[data-case-status]');if(st&&S.caseId){patchCase(S.caseId,{status:st.dataset.caseStatus},'ESTADO_CAMBIADO');render();return}const pg=e.target.closest('[data-page]');if(pg){S.page+=Number(pg.dataset.page||0);render();return}const em=e.target.closest('[data-entity-mode]');if(em){S.entityMode=em.dataset.entityMode;render();return}const cp=e.target.closest('[data-copy-report]');if(cp&&S.caseId){const c=getCase(S.caseId);navigator.clipboard?.writeText(textReport(c)).then(()=>{cp.textContent='Copiado';setTimeout(()=>cp.textContent='Copiar síntesis',1200)}).catch(()=>{});}});
document.addEventListener('change',e=>{if(e.target.matches('[data-evidence-status]')&&S.caseId){const c=getCase(S.caseId);const i=Number(e.target.dataset.evidenceStatus);if(c?.evidence?.[i]){c.evidence[i].status=e.target.value;patchCase(S.caseId,{evidence:c.evidence},'EVIDENCIA_ACTUALIZADA');renderKpis()}}});
document.addEventListener('focusout',e=>{if(!S.caseId)return;if(e.target.id==='hypothesis')patchCase(S.caseId,{hypothesis:e.target.value},'HIPOTESIS_ACTUALIZADA');if(e.target.id==='caseNotes')patchCase(S.caseId,{notes:e.target.value},'NOTA_ACTUALIZADA');if(e.target.id==='caseConclusion')patchCase(S.caseId,{conclusion:e.target.value},'CONCLUSION_ACTUALIZADA')});
$('search').addEventListener('input',e=>{S.query=e.target.value;S.page=0;if(S.view==='triage')render()});
document.querySelectorAll('[data-level]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-level]').forEach(x=>x.classList.remove('on'));b.classList.add('on');S.level=b.dataset.level;S.page=0;if(S.view!=='triage')S.view='triage';render()}));
fetch(DATA_URL,{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('HTTP '+r.status);return r.json()}).then(d=>{S.payload=d;S.rows=Array.isArray(d.relation_findings)?d.relation_findings:[];setStatus(`${fmt.format(S.rows.length)} relaciones priorizadas cargadas · arquitectura por expediente`);$('guardrail').textContent=d.guardrail||'RIGP prioriza revisión y no acredita irregularidad, delito ni responsabilidad.';render()}).catch(err=>{setStatus('No fue posible cargar el producto analítico.');$('workspace').innerHTML=`<div class="empty large"><h2>Error de carga</h2><p>${esc(err.message)}</p></div>`});
})();