(()=>{'use strict';
const URL='data/benefit_context.json';
const LINK_KEY='rigp_benefit_links_v1';
let cache=null,active=false,focus={service:null,provider:null};
const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const fmt=new Intl.NumberFormat('es-CL');
const money=v=>{v=Number(v||0);if(!v)return '—';const a=Math.abs(v);if(a>=1e12)return '$'+new Intl.NumberFormat('es-CL',{maximumFractionDigits:1}).format(v/1e12)+' bill.';if(a>=1e9)return '$'+new Intl.NumberFormat('es-CL',{maximumFractionDigits:1}).format(v/1e9)+' mil M';if(a>=1e6)return '$'+new Intl.NumberFormat('es-CL',{maximumFractionDigits:1}).format(v/1e6)+' M';return '$'+fmt.format(Math.round(v))};
const KIND={PERSONA:'Persona',SOCIEDAD:'Sociedad',NO_DETERMINADO:'Tipo no determinado'};
const STATUS={POR_VERIFICAR:'Por verificar',VINCULO_CONFIRMADO:'Vínculo confirmado',DESCARTADO:'Descartado'};
const ROLE={CONTROL:'Propiedad o control',REPRESENTACION:'Representación legal',DIRECCION:'Dirección o administración',RELACION_ECONOMICA:'Relación económica documentada',OTRO:'Otra vinculación documentada'};
function saved(){try{return JSON.parse(localStorage.getItem(LINK_KEY)||'{}')}catch{return {}}}
function writeSaved(v){localStorage.setItem(LINK_KEY,JSON.stringify(v))}
async function load(){if(cache)return cache;const r=await fetch(URL,{cache:'no-store'});if(!r.ok)throw new Error('No fue posible cargar el contexto de beneficiarios');cache=await r.json();return cache}
function candidates(){return Object.values(cache?.candidates||{})}
function parseContext(){const spans=[...document.querySelectorAll('#contextbar span')];let serviceName='',providerName='';for(const el of spans){const t=el.textContent.trim();if(t.startsWith('Servicio:'))serviceName=t.slice(9).trim();if(t.startsWith('Proveedor:'))providerName=t.slice(10).trim()}let service=null,provider=null;if(serviceName){const x=Object.values(cache?.services||{}).find(v=>String(v.organization_name||'').trim()===serviceName);service=x?String(x.organization_id):null}if(providerName){const x=candidates().find(v=>String(v.provider_name||'').trim()===providerName);provider=x?String(x.provider_id):null}return {service,provider}}
function candidate(pid){return cache?.candidates?.[pid]||null}
function service(sid){return cache?.services?.[sid]||null}
function providerLinks(pid){return saved()[pid]||[]}
function confirmedLinks(pid){return providerLinks(pid).filter(x=>x.status==='VINCULO_CONFIRMADO')}
function scoreLabel(n){if(n>=75)return 'Muy alta';if(n>=60)return 'Alta';if(n>=40)return 'Media';return 'Inicial'}
function syncMainProvider(pid){const b=document.createElement('button');b.hidden=true;b.dataset.provider=pid;document.body.appendChild(b);b.click();b.remove()}
function syncMainService(sid){const b=document.createElement('button');b.hidden=true;b.dataset.service=sid;document.body.appendChild(b);b.click();b.remove()}
function setNav(){document.querySelectorAll('.nav button').forEach(b=>b.classList.remove('on'));document.querySelector('[data-benefit-view]')?.classList.add('on')}
function hideGlobal(){const t=$('filtersWrap'),k=$('kpis');if(t)t.style.display='none';if(k)k.style.display='none'}
function showGlobal(){const t=$('filtersWrap'),k=$('kpis');if(t)t.style.display='';if(k)k.style.display=''}
function exitBenefit(){if(!active)return;active=false;showGlobal();document.querySelector('[data-benefit-view]')?.classList.remove('on')}
function stageState(pid){const links=providerLinks(pid),confirmed=confirmedLinks(pid);return [
 {n:1,title:'Receptor económico directo',state:'Disponible',cls:'ok',text:'La persona o sociedad aparece directamente como proveedor/receptor de recursos públicos en relaciones priorizadas.'},
 {n:2,title:'Propiedad, control y administración',state:confirmed.length?'Con antecedentes':'Por verificar',cls:confirmed.length?'mid':'todo',text:confirmed.length?`${confirmed.length} vínculo(s) documentado(s) por el analista. La cobertura automática de control societario aún no está integrada.`:'Aún no existe fuente integrada de socios/accionistas, controladores, representantes, directores o administradores.'},
 {n:3,title:'Personas y sociedades vinculadas',state:links.length?`${links.length} registrado(s)`:'Sin registrar',cls:links.length?'mid':'todo',text:'Sólo deben incorporarse vínculos respaldados por una fuente o documento verificable.'},
 {n:4,title:'Beneficio final',state:'No determinado',cls:'todo',text:'No se atribuye beneficio final sin evidencia de propiedad/control, disposición del valor o transferencias posteriores.'}
 ]}
function topList(arr,label){return (arr||[]).slice(0,6).map(x=>`<div class="benefit-line"><span><b>${esc(x.organization_name||x.provider_name||'Entidad')}</b><small>${esc(label)}</small></span><em>${money(x.amount_clp)}</em></div>`).join('')}
function markList(c){const m=c?.tax_profile?.marks||[];if(!m.length)return '<p class="benefit-muted">Sin marcas tributarias compactas para este receptor.</p>';return m.map(x=>`<div class="benefit-mark"><span class="sev ${esc(String(x.severity||'LOW').toLowerCase())}">${esc(x.severity||'Contexto')}</span><div><b>${esc(x.signal_type||'Señal tributaria')}</b><p>${esc(x.why||'')}</p></div></div>`).join('')}
function findingList(c){return (c?.findings||[]).slice(0,8).map(r=>`<button class="benefit-finding" data-open="${esc(r.finding_id||'')}"><span><b>${esc(r.finding_title||'Hallazgo priorizado')}</b><small>${esc(r.organization_name||'Servicio')} · ${esc(r.periodo||'')} · ${Math.round(Number(r.priority_score||0))}/100</small></span><em>Abrir</em></button>`).join('')||'<p class="benefit-muted">Sin hallazgos asociados.</p>'}
function linkRows(pid){const rows=providerLinks(pid);if(!rows.length)return '<p class="benefit-muted">Todavía no hay personas o sociedades vinculadas registradas para este receptor.</p>';return rows.map((x,i)=>`<article class="benefit-link ${esc(String(x.status||'POR_VERIFICAR').toLowerCase())}"><div><div class="benefit-link-top"><span>${esc(x.entity_type==='PERSONA'?'Persona':'Sociedad')}</span><span>${esc(STATUS[x.status]||x.status)}</span></div><h4>${esc(x.name)}</h4><p><b>${esc(ROLE[x.role]||x.role)}</b> · ${esc(x.source)}</p>${x.note?`<p>${esc(x.note)}</p>`:''}</div><div class="benefit-link-actions"><button data-link-status="${i}|VINCULO_CONFIRMADO">Confirmar vínculo</button><button data-link-status="${i}|DESCARTADO">Descartar</button><button data-link-delete="${i}">Eliminar</button></div></article>`).join('')}
function hypothesis(c){const who=c.entity_kind==='PERSONA'?'esta persona':'esta entidad';return `Si los hechos que originan los hallazgos fueran posteriormente acreditados como impropios, ${who} sería el receptor económico directo que corresponde examinar primero. Eso no permite atribuir por sí solo beneficio ilícito ni beneficio final a sus socios, representantes, administradores u otras personas relacionadas.`}
function renderDetail(c){const stages=stageState(c.provider_id),tax=c.tax_profile||{},links=providerLinks(c.provider_id),confirmed=confirmedLinks(c.provider_id);$('workspace').innerHTML=`<div class="section-head benefit-head"><div><div class="eyebrow">Beneficiarios y vínculos</div><h2>${esc(c.provider_name)}</h2><p>Reconstrucción progresiva desde el receptor económico directo hacia personas o sociedades que sólo pueden vincularse con evidencia verificable.</p></div><span>${esc(KIND[c.entity_kind]||c.entity_kind)} · ${esc(c.rut||'RUT no disponible')}</span></div>
<section class="benefit-metrics"><div><b>${Math.round(Number(c.verification_priority||0))}/100</b><span>prioridad de verificación</span></div><div><b>${fmt.format(c.finding_count||0)}</b><span>hallazgos</span></div><div><b>${fmt.format(c.services_in_findings||0)}</b><span>servicios</span></div><div><b>${money(c.relation_amount_published)}</b><span>flujo en pares con cobertura</span></div><div><b>${confirmed.length}</b><span>vínculos confirmados</span></div></section>
<div class="benefit-grid"><section class="benefit-panel wide"><div class="benefit-panel-head"><div><div class="eyebrow">Cadena de beneficio</div><h3>Qué está acreditado y qué falta reconstruir</h3></div><span>${esc(scoreLabel(c.verification_priority))}</span></div><div class="benefit-stages">${stages.map(s=>`<div class="benefit-stage ${s.cls}"><i>${s.n}</i><div><div class="benefit-stage-top"><b>${esc(s.title)}</b><span>${esc(s.state)}</span></div><p>${esc(s.text)}</p></div></div>`).join('')}</div></section>
<section class="benefit-panel"><div class="eyebrow">Hipótesis guiada</div><h3>Qué puede sostenerse hoy</h3><p class="benefit-hypothesis">${esc(hypothesis(c))}</p><div class="benefit-check"><b>Comprobar después</b><ol><li>Quiénes controlaban, administraban o representaban al receptor durante el periodo relevante.</li><li>Si hubo cambios societarios cercanos a adjudicaciones, convenios o pagos priorizados.</li><li>Si existen otras sociedades bajo control común que también aparecen recibiendo recursos públicos.</li><li>Si existe evidencia documental o financiera de transferencia posterior del valor a otra persona o sociedad.</li></ol></div></section>
<section class="benefit-panel"><div class="eyebrow">Captura económica publicada</div><h3>Magnitud y recurrencia</h3><div class="benefit-facts"><div><span>Gasto público total publicado</span><b>${money(c.total_public_spend_published)}</b></div><div><span>Monto máximo de transacción asociada</span><b>${money(c.max_transaction_amount)}</b></div><div><span>Atención inmediata</span><b>${fmt.format(c.immediate_count||0)}</b></div><div><span>Familias convergentes</span><b>${fmt.format((c.families||[]).length)}</b></div></div><div class="benefit-lines">${topList(c.top_services,'Servicio relacionado')}</div></section>
<section class="benefit-panel"><div class="eyebrow">Caracterización tributaria</div><h3>${tax.available?'Contexto SII disponible':'Cobertura SII no disponible'}</h3>${tax.available?`<div class="benefit-facts"><div><span>Razón social publicada</span><b>${esc(tax.legal_name||c.provider_name)}</b></div><div><span>Estado tributario</span><b>${esc(tax.tax_status||'—')}</b></div><div><span>Actividad principal</span><b>${esc(tax.main_activity||'—')}</b></div><div><span>Región</span><b>${esc(tax.main_region||'—')}</b></div><div><span>Tramo ventas</span><b>${esc(tax.sales_band_label||'—')}</b></div><div><span>Trabajadores</span><b>${esc(tax.workers??'—')}</b></div></div>${markList(c)}`:'<p class="benefit-muted">El receptor no tiene caracterización tributaria compacta disponible en este piloto.</p>'}</section>
<section class="benefit-panel wide"><div class="benefit-panel-head"><div><div class="eyebrow">Personas y sociedades vinculadas</div><h3>Registro de antecedentes documentados</h3></div><span>Piloto local</span></div><p class="benefit-muted">Registrar un vínculo no significa atribuir beneficio ilícito. Para confirmarlo debe existir una fuente o documento concreto.</p><div class="benefit-link-list">${linkRows(c.provider_id)}</div><div class="benefit-form"><select id="benefitEntityType"><option value="PERSONA">Persona</option><option value="SOCIEDAD">Sociedad</option></select><input id="benefitName" placeholder="Nombre de persona o sociedad"><select id="benefitRole"><option value="CONTROL">Propiedad o control</option><option value="REPRESENTACION">Representación legal</option><option value="DIRECCION">Dirección o administración</option><option value="RELACION_ECONOMICA">Relación económica documentada</option><option value="OTRO">Otra vinculación documentada</option></select><input id="benefitSource" placeholder="Fuente o documento que respalda el vínculo"><textarea id="benefitNote" rows="2" placeholder="Nota breve (opcional)"></textarea><button data-benefit-add-link>Agregar vínculo por verificar</button></div></section>
<section class="benefit-panel wide"><div class="benefit-panel-head"><div><div class="eyebrow">Hallazgos que originan la revisión</div><h3>Volver siempre a la evidencia base</h3></div><span>${fmt.format(c.finding_count||0)} hallazgos</span></div><div class="benefit-findings">${findingList(c)}</div></section></div>
<div class="benefit-actions"><button data-benefit-network>Ver red investigativa</button><button data-benefit-explore>Explorar contexto económico</button></div><p class="benefit-guard">La prioridad de verificación organiza a quién revisar primero. No es probabilidad de delito ni acredita que una persona o sociedad haya obtenido un beneficio ilícito. Un proveedor/receptor directo no equivale a beneficiario final.</p>`}
function card(c){return `<article class="benefit-card"><div class="benefit-card-top"><span>${esc(KIND[c.entity_kind]||c.entity_kind)}</span><b>${Math.round(Number(c.verification_priority||0))}/100</b></div><h3>${esc(c.provider_name)}</h3><p>${esc(c.rut||'RUT no disponible')}</p><div class="benefit-card-stats"><div><b>${c.finding_count||0}</b><span>hallazgos</span></div><div><b>${c.services_in_findings||0}</b><span>servicios</span></div><div><b>${c.immediate_count||0}</b><span>inmediatas</span></div></div><p>Receptor económico directo en relaciones priorizadas. Prioridad para reconstruir propiedad/control y eventual destino final del beneficio.</p><button data-benefit-provider="${esc(c.provider_id)}">Examinar cadena</button></article>`}
function renderService(sid){const s=service(sid);if(!s){renderNoFocus();return}const list=(s.candidate_providers||[]).map(candidate).filter(Boolean);$('workspace').innerHTML=`<div class="section-head"><div><div class="eyebrow">Beneficiarios y vínculos</div><h2>${esc(s.organization_name)}</h2><p>Receptores económicos directos vinculados a hallazgos del servicio. El objetivo es decidir en cuáles conviene reconstruir propiedad, control y destino del beneficio.</p></div><span>${list.length} receptores</span></div><section class="benefit-cards">${list.map(card).join('')||'<div class="empty">Sin receptores priorizados para este servicio.</div>'}</section><p class="benefit-guard">Esta vista prioriza receptores económicos para verificación. No identifica beneficiarios ilícitos ni responsables.</p>`}
function renderNoFocus(){const all=candidates().sort((a,b)=>Number(b.verification_priority||0)-Number(a.verification_priority||0));const cov=cache?.coverage||{};$('workspace').innerHTML=`<div class="section-head"><div><div class="eyebrow">Beneficiarios y vínculos</div><h2>Quién recibe directamente los recursos priorizados</h2><p>Primer paso para llegar a personas o sociedades que eventualmente pudieran capturar un beneficio: identificar al receptor económico directo y luego reconstruir, con evidencia, propiedad, control y destino del valor.</p></div><span>${all.length} receptores directos</span></div><section class="benefit-metrics"><div><b>${fmt.format(cov.direct_recipients||0)}</b><span>receptores directos</span></div><div><b>${fmt.format(cov.classified_companies||0)}</b><span>sociedades clasificadas</span></div><div><b>${fmt.format(cov.classified_people||0)}</b><span>personas clasificadas</span></div><div><b>${fmt.format(cov.sii_matched||0)}</b><span>con contexto SII</span></div><div><b>0</b><span>fuentes de control integradas</span></div></section><section class="benefit-cards">${all.slice(0,24).map(card).join('')||'<div class="empty">Sin receptores disponibles.</div>'}</section><p class="benefit-guard">La prioridad de verificación no es un score de delito. El receptor directo puede ser plenamente legítimo; la cadena sólo orienta qué antecedentes conviene verificar después.</p>`}
function render(){if(!active)return;if(focus.provider){const c=candidate(focus.provider);if(c){renderDetail(c);return}}if(focus.service){renderService(focus.service);return}renderNoFocus()}
function enter(){const home=document.querySelector('[data-view="inicio"]');if(home)home.click();active=true;setNav();hideGlobal();$('workspace').innerHTML='<div class="empty">Cargando contexto de beneficiarios…</div>';load().then(()=>{const p=parseContext();if(p.service||p.provider)focus=p;render()}).catch(e=>{$('workspace').innerHTML=`<div class="empty">${esc(e.message||e)}</div>`})}
function setProvider(pid){syncMainProvider(pid);focus={service:null,provider:pid};setNav();hideGlobal();render()}
function addLink(){if(!focus.provider)return;const name=$('benefitName')?.value.trim()||'',source=$('benefitSource')?.value.trim()||'';if(name.length<3){$('benefitName')?.focus();return}if(source.length<5){$('benefitSource')?.focus();return}const all=saved(),rows=all[focus.provider]||[];rows.push({entity_type:$('benefitEntityType')?.value||'PERSONA',name,role:$('benefitRole')?.value||'OTRO',source,note:$('benefitNote')?.value.trim()||'',status:'POR_VERIFICAR',created_at:new Date().toISOString(),updated_at:new Date().toISOString()});all[focus.provider]=rows;writeSaved(all);render()}
function updateLink(token){if(!focus.provider)return;const [idxRaw,status]=String(token||'').split('|'),idx=Number(idxRaw),all=saved(),rows=all[focus.provider]||[];if(!rows[idx])return;rows[idx]={...rows[idx],status,updated_at:new Date().toISOString()};all[focus.provider]=rows;writeSaved(all);render()}
function deleteLink(idx){if(!focus.provider)return;const all=saved(),rows=all[focus.provider]||[];rows.splice(Number(idx),1);all[focus.provider]=rows;writeSaved(all);render()}
function toNetwork(){exitBenefit();document.querySelector('[data-network-view]')?.click()}
function toExplore(){exitBenefit();document.querySelector('[data-view="explorar"]')?.click()}
document.addEventListener('click',e=>{const v=e.target.closest('[data-benefit-view]');if(v){e.preventDefault();enter();return}if(!active)return;if(e.target.closest('[data-network-view]')){exitBenefit();return}const p=e.target.closest('[data-benefit-provider]');if(p){setProvider(p.dataset.benefitProvider);return}if(e.target.closest('[data-benefit-add-link]')){addLink();return}const st=e.target.closest('[data-link-status]');if(st){updateLink(st.dataset.linkStatus);return}const del=e.target.closest('[data-link-delete]');if(del){deleteLink(del.dataset.linkDelete);return}if(e.target.closest('[data-benefit-network]')){toNetwork();return}if(e.target.closest('[data-benefit-explore]')){toExplore();return}},true);
document.addEventListener('click',e=>{if(active&&e.target.closest('[data-view]'))exitBenefit()},false);
})();
